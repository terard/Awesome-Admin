This a simple to use application to maintain system user passwords and the security roles each user may possess.

It should:
- allow the application user to change a users password.
- display the list of users in a table form with an indicator in the appropriate cell to show a user possesses a role.

> It has a command line interface to the application.
