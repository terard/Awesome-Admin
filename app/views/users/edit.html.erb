<%= form_for @user do |f| %>
  <fieldset>
    <legend>Change Password</legend>

    <%= f.label "New Password" %>
    <%= f.text_field :password %>
    <%= f.submit 'Change' %>  
    
  </fieldset>
<% end %>
